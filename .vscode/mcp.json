{
  // MCP server configuration for VS Code Copilot Chat
  // After running the server locally (npm run dev), this connects VS Code to it.
  "servers": {
    "mcp-todo-app": {
      "type": "http",
      "url": "http://localhost:8000/mcp",
      "headers": {}
    }
  }
}
